# El host no necesita estas vars: las inyecta docker compose.
# Sobreescribí solo si corrés un cliente fuera de Docker.

NEXT_PUBLIC_API_URL=http://localhost:8080
EXPO_PUBLIC_API_URL=http://localhost:8080
HOST_IP=127.0.0.1
